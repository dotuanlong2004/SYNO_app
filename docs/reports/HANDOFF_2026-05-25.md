# SYNO Handoff - 2026-05-25

## Git State

- Branch: `main`
- Remote: `origin https://github.com/dotuanlong2004/SYNO_app.git`
- Latest pushed commit: `bec51701 chore: stabilize SYNO admin platform progress`
- Push result confirmed by user: `f99660a2..bec51701 main -> main`
- Repo status after push: `main...origin/main`, no local ahead commit.

## What Was Completed

- Read and followed `AGENTS.md`, external Windsurf/Cline context where readable, and project status docs.
- Recovered prior task context: Super Admin separation and platform admin progress were the active track.
- Fixed Admin/Super Admin `Failed to fetch` root cause by confirming backend must run on port `3000`.
- Kept separate apps:
  - `admin_web` for school-scoped `admin` and `teacher`.
  - `super_admin_web` for platform-scoped `super_admin`.
- Added/finished Super Admin Web surface with platform admin APIs under `/api/v1/platform-admin/*`.
- Improved Admin Web and Super Admin Web UI:
  - summary cards;
  - search/filter controls;
  - empty states;
  - Vietnamese role/status labels;
  - currency/date formatting helpers;
  - favicon/title cleanup.
- Added backend contract coverage for admin account/school validation.
- Confirmed Super Admin account rule:
  - `super_admin` has `school_id = null`;
  - test login: `superadmin@syno.local` / `123456`.
- Organized docs under `docs/` and removed deprecated active workspace paths for old `zk-agent` / `ZKCollector` flows.
- Added AI-X1 collector run helper:
  - `hardware-collector/ronald-jack-aix1/run-collector.ps1`
  - reads `HARDWARE_API_KEY` from `backend/.env`;
  - sets `COLLECTOR_REQUIRE_HARDWARE_API_KEY=true`;
  - refuses to run if key is missing.

## Verification Evidence

Fresh checks run before commit:

- `corepack pnpm --filter backend test`: passed.
- `corepack pnpm --filter admin_web run typecheck`: passed.
- `corepack pnpm --filter super_admin_web run typecheck`: passed.
- `corepack pnpm --filter admin_web run build`: passed, Vite chunk-size warning only.
- `corepack pnpm --filter super_admin_web run build`: passed.
- `corepack pnpm --filter backend run check:production`: passed with current local `.env`.
- `dotnet build d:\attendance_app_dev\hardware-collector\ronald-jack-aix1 -c Release`: passed with 0 warnings and 0 errors.
- `flutter analyze` from `attendance_app`: passed, no issues.
- `flutter test` from `attendance_app`: passed. It logs a caught FCM initialization warning in test context, but tests still pass.
- API smoke with live backend:
  - `/health`: `ok:true`, Supabase `up`, queue `enabled`.
  - Admin Web students returned `3`.
  - Platform schools returned `1`.
  - Platform admin users returned `3`.
  - Platform audit logs returned `1`.
- `git diff --cached --check`: passed before commit.

## Current Run URLs

Use these when local servers are running:

- Backend: `http://127.0.0.1:3000`
- Admin Web: `http://localhost:5173/`
- Super Admin Web: `http://127.0.0.1:5174/`

Common local accounts:

- Super Admin: `superadmin@syno.local` / `123456`
- School Admin: `admin@school.edu` / `admin123`

## Next Recommended Work

1. Run a real Firebase end-to-end device-token push test.
2. Continue Admin Web and Parent App feature polish for timetable, grades, fees, announcements, and attendance UX.
3. Verify AI-X1 collector through `run-collector.ps1` with the real backend `HARDWARE_API_KEY`.
4. Keep platform/school separation strict:
   - Super Admin routes stay under `/api/v1/platform-admin/*`.
   - School Admin routes stay under `/api/v1/admin-web/*`.
   - No frontend should receive `service_role`.
   - Every business query remains scoped by `school_id`.
5. Before any "done" claim, rerun the relevant verification commands and record evidence.

## Important Notes For Tomorrow

- Do not restore old `zk-agent`, Node local agent, MITA/ZK scripts, or archived markdown as active workflow.
- Current hardware path is `hardware-collector/ronald-jack-aix1`.
- Current docs source of truth remains:
  - `AGENTS.md`
  - `CURRENT_STATUS.md`
  - `PROJECT_MEMORY_SYNO.md`
  - `HUONG_DAN_CHAY_AI_X1.md`
  - `ROADMAP.md`
- Local git was clean after the pushed commit. If it becomes dirty tomorrow, inspect with `git status --short` and classify changes before editing.
